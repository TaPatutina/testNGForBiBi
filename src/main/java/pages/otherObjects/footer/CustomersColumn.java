package pages.otherObjects.footer;

import org.openqa.selenium.WebDriver;
import pages.base.BasePage;

public class CustomersColumn extends BasePage {
    public CustomersColumn(WebDriver driver) {
        super(driver);
    }

    //  клик на ссылку - редирект на релевантную тексту ссылки страницу
    //  (checkRedirectFromAboutLink)
    //  altText при наведении курсора на линк
    // + проверка линка на ВК из след. столбца


}
