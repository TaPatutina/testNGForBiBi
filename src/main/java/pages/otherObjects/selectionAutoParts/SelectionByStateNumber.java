package pages.otherObjects.selectionAutoParts;

import org.openqa.selenium.WebDriver;
import pages.base.BasePage;

public class SelectionByStateNumber extends BasePage {
    public SelectionByStateNumber(WebDriver driver) {
        super(driver);
    }
}
