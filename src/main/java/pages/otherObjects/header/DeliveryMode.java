package pages.otherObjects.header;

import org.openqa.selenium.WebDriver;
import pages.base.BasePage;

public class DeliveryMode extends BasePage {
    public DeliveryMode(WebDriver driver) {
        super(driver);
    }
}
