package pages.otherObjects.header;

import org.openqa.selenium.WebDriver;
import pages.base.BasePage;

public class TopFindForeignProduct extends BasePage {
    public TopFindForeignProduct(WebDriver driver) {
        super(driver);
    }
}
