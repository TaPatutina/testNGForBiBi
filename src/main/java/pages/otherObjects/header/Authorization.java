package pages.otherObjects.header;

import org.openqa.selenium.WebDriver;
import pages.base.BasePage;

public class Authorization extends BasePage {
    Authorization(WebDriver driver) {
        super(driver);
    }
}
