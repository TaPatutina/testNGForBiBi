package pages.otherObjects.header;

import org.openqa.selenium.WebDriver;
import pages.base.BasePage;

public class MainCatalog extends BasePage {
    public MainCatalog(WebDriver driver) {
        super(driver);
    }
}
