package pages.homePage.header;

import org.openqa.selenium.WebDriver;
import pages.base.BasePage;

public class BaseStoreMode extends BasePage {
    public BaseStoreMode(WebDriver driver) {
        super(driver);
    }
}
